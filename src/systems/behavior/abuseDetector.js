class AbuseDetector {
    constructor(client) {
        this.client = client;
    }

    check(message) {
        // Placeholder — real NLP logic added later
        return false;
    }
}

module.exports = { AbuseDetector };
