class AIDetector {
    constructor(client) {
        this.client = client;
    }

    async isAI(attachment) {
        // Placeholder — real logic added later
        return false;
    }
}

module.exports = { AIDetector };
